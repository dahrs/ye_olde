# Copy to search_api/.env for local development. In production (the HF
# Space), these are set as Space secrets/variables instead — see spec §10.

HF_DATASET_REPO_ID=            # e.g. dahrs/ye-olde-index
HF_TOKEN=                      # only needed if the dataset repo is private
