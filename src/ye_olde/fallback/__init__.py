"""Native -> loan -> constructed -> temporal-loan fallback chain — spec §2, §3a.

One mechanism reused for two phenomena: language contact (loanwords, gated on
first_borrowing_year) and language-vs-concept-age mismatch (anachronisms, i.e.
zero attestation for the entire sense_id). Not yet implemented — the
"constructed compound" rung is flagged in spec §8 as still under-specified.
"""
