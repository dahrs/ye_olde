"""Resolves (lang_code, year) to weighted corpus partitions — spec §2, §3a.

The only place "which named historical period is this" gets decided;
everything downstream sees weighted corpus partitions, not era labels.
Not yet implemented.
"""
